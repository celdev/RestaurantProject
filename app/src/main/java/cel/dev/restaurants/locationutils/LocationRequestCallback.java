package cel.dev.restaurants.locationutils;

/** Callback which allows for moving some location result methods out of the views
 * */
public interface LocationRequestCallback {
    void requestLocationCallback();
}
