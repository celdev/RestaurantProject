<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MaterialButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/ok_blue_highlight</item>
        <item name="colorButtonNormal">@color/ok_blue</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MaterialButtonDanger" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/danger_red_highlight</item>
        <item name="colorButtonNormal">@color/danger_red</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
